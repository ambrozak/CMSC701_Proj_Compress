#include <iostream>
#include <string>
#include <fstream>
#include <vector>
#include <map>
#include <cstring>

using namespace std;


int main(int argc, char* argv[]) {

    if (argc != 3) {
        printf("Use: compress reference_compressed.fa queries.fa\n");
        return 1;
    }

    // Load ref

    // Load and search for queries

    // Write querying results
    
    return 0;
}