#include <iostream>
#include <string>
#include <fstream>
#include <vector>
#include <map>
#include <cstring>

using namespace std;


int main(int argc, char* argv[]) {

    if (argc != 3) {
        printf("Use: compress in_ref.fa out_compressed.fa\n");
        return 1;
    }

    // Load

    // Compress

    // Write out
    
    return 0;
}